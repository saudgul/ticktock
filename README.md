# TickTock

## Setup Instructions

1. Install Node.js (version 18 or higher).
2. Install project dependencies:

```bash
npm install
```

3. Start the development server:

```bash
npm run dev
```

4. Build the project for production:

```bash
npm run build
```

5. Preview the production build:

```bash
npm run preview
```

## Frameworks/Libraries Used

- React 18 (UI)
- React Router DOM (routing)
- Vite 5 (build tool and dev server)
- Tailwind CSS (styling)
- Radix UI: @radix-ui/react-toast, @radix-ui/react-tooltip (UI primitives)
- Lucide React (icons)
- Utility libraries: class-variance-authority, clsx, tailwind-merge, tailwindcss-animate
- Tooling: ESLint, Vitest, jsdom, PostCSS, Autoprefixer

## Time Spent

- Total time spent on this project: 28 hours